# TODO: parse config.py and create watchdog observer and handlers as defined
import time

from config import CONFIG


if __name__ == "__main__":
    while True:
        print("Idle...")
        time.sleep(1)
